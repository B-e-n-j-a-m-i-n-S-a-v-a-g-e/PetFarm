

interface IGuineaPig {

    eat(quantity: number);
    bweep(numSweeps: number);
    poop(quantity: number);
    getX();
    setX(x: number);
    getY();
    setY(x: number);
    getLength();
    getWidth();
    getColor();

}